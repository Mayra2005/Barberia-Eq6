CREATE TABLE Clientes (
    idClientes INT AUTO_INCREMENT,
    nombre VARCHAR(50),
    paterno VARCHAR(50),
    materno VARCHAR(50),
    telefono VARCHAR(10),
    correo VARCHAR(50),
    PRIMARY KEY (idClientes)
);
